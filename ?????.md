# 阿臭弟AI发送器使用说明书

## 1. 软件用途

阿臭弟AI发送器是一个本地运行的 AI 网页自动发送工具，用于把多阶段提示词、图片、文件按顺序发送到 ChatGPT、Gemini、豆包、DeepSeek 或自定义网页。

它不会自带 AI 账号，也不会绕过网页登录。每台电脑第一次使用时，需要在软件打开的目标网页里正常登录对应账号。

## 2. 便携版启动方式

1. 解压压缩包到本地英文或中文路径均可，建议路径不要太深。
2. 双击 `阿臭弟AI发送器.exe` 启动，不需要安装 Node.js，也不需要管理员权限。
3. 启动后左侧选择浏览器、资料目录和目标平台。
4. 点击 `新开 ChatGPT 窗口`、`新开 Gemini 窗口` 等按钮，先完成网页登录。
5. 回到软件填写阶段内容，点击 `开始发送`。

必须先完整解压再运行。`阿臭弟AI发送器.exe` 和 `app-data` 必须保持在同一目录，不能只单独移动 exe。

## 3. 首次使用流程

1. 浏览器选择：优先选择 Microsoft Edge 或 Google Chrome。
2. 资料目录选择：建议使用 `独立资料目录（推荐）`。
3. 资料目录名称：默认 `Default` 即可；如果需要多个账号，可填写不同名称，例如 `账号1`、`账号2`。
4. 目标平台：选择要发送到的 AI 网站。
5. 新开窗口：点击对应平台的新开窗口按钮，并在弹出的浏览器中登录。
6. 工作流：选择已有模板，或新建内容后点击保存。
7. 任务：一个任务可以包含多个阶段，按顺序执行，遇到第一个空阶段停止。

## 4. 工作流模板

工作流模板保存在：

`app-data\workflows`

如果需要把模板迁移到另一台电脑，只复制这个文件夹即可。

朋友版已经附带打包时确认的工作流模板；模板只包含工作流结构和提示词，不包含浏览器登录状态。

保存规则：

- `保存`：把当前页面中的阶段、提示词、变量替换规则保存为工作流。
- `重载`：重新读取当前选中的工作流。
- `删除`：删除当前选中的工作流文件。
- 选择工作流后会自动加载，不需要再额外点击读取。

注意：图片和文件不会作为永久模板内容保存。重新打开后需要重新选择图片或文件。

## 5. 图片和文件上传

每个阶段左侧是产品图或文件区域：

- 可以点击选择文件。
- 可以拖入单张图片或多个文件。
- 可以拖入文件夹，软件会自动拆出里面的图片和文件，并按文件名顺序排列。
- 缩略图可以拖拽排序。
- 单张图片可以单独删除。

## 6. 变量替换规则

每个阶段可以填写：

- 原内容：原始提示词模板。
- 替换的原内容：要替换的变量名或原文片段。
- 替换内容：替换后的内容。
- 全新内容：软件自动生成，实际发送给网页的是这里的最终结果。

变量支持直接填写变量名，不强制手动输入特殊符号。

示例：

原内容中有：

`【填写产品名称、类目、品牌、型号、SKU、颜色】`

替换的原内容可以只填：

`填写产品名称、类目、品牌、型号、SKU、颜色`

替换内容填写：

`蓝色硅胶收纳盒，厨房用品，三件套`

最终发送时会使用替换后的全新内容。

## 7. 同浏览器多任务排队

同一个浏览器、同一个资料目录、同一个 AI 平台下，可以添加多个任务。

规则：

- 新增任务只是创建任务页，不会自动开始。
- 每个任务都需要点击一次 `开始发送` 才会进入队列。
- 进入队列后不需要继续操作。
- 同一浏览器资料目录下任务会自动排队执行。
- 不同浏览器或不同资料目录可以并行执行。

状态说明：

- `未提交`：还没有点击开始，不会执行。
- `排队中`：已提交，等待前一个任务完成。
- `执行中`：当前正在发送。
- `已完成`：任务全部阶段执行完毕。
- `失败`：网页、浏览器、登录或页面状态出现问题。

失败保护规则：

- 当前任务输出未通过验收时，同一浏览器资料目录后面的任务会暂停，避免回合继续错位。
- 点击失败任务右侧的 `续跑`，会从第一个未完成阶段继续。
- 点击 `重跑`，会从第 1 阶段重新开始。
- 先处理失败任务，再让同一队列继续；不要在网页里手动插入其他对话内容。

## 8. 数据目录说明

便携版会在软件同级目录自动生成：

`app-data`

里面主要包含：

- `workflows`：工作流模板。
- `browser-profiles`：独立浏览器登录资料。
- `runs`：任务运行过程文件。
- `tmp_uploads`：临时上传文件。

不要随意删除 `browser-profiles`，否则独立资料目录里的网页登录状态会丢失。

如果要把软件发给别人，建议不要带 `browser-profiles`，避免把自己的账号状态带出去。

本朋友版只预置 `app-data\workflows`，不包含制作者的 `browser-profiles`、`runs`、`tmp_uploads`、日志或历史任务。

## 9. 常见问题

### 打开后没有登录状态

这是正常情况。独立资料目录不会读取你原本手动打开浏览器的登录状态，需要第一次单独登录。

### 点击开始后失败

优先检查：

1. 是否已经在目标 AI 网页登录。
2. 浏览器是否被手动关闭。
3. 目标网页是否能正常输入文字。
4. 当前阶段是否选择了需要上传的图片。
5. 是否同一账号同时开了过多自动任务。

### 工作流没有带图片

这是正常规则。模板只保存提示词和结构，图片需要每次任务重新选择。

### 多台电脑迁移

推荐迁移方式：

1. 解压便携包。
2. 第一次启动软件。
3. 登录目标 AI 网站。
4. 使用自带工作流或复制旧电脑的 `app-data\workflows`。

不要直接复制旧电脑的 `browser-profiles` 给别人使用。

### Windows 提示“未知发布者”

当前便携程序未配置商业代码签名，Windows 可能进行安全扫描或显示未知发布者。请只使用发送者提供的原始压缩包，并用包内或同目录的 SHA256 信息核对文件完整性。

### 生成结果保存

软件只负责按阶段上传和发送，不会自动下载 AI 网页生成的图片或文件。结果需要在目标 AI 网页中手动保存。

## 10. 风控建议

同一账号不建议同时跑大量任务。更稳的方式是：

- 一个浏览器资料目录对应一个账号。
- 同账号任务使用排队，不要并行抢同一个窗口。
- 不同账号、不同浏览器资料目录可以并行。
- 遇到平台提示异常活动时，先暂停任务，手动完成验证或等待账号恢复。
